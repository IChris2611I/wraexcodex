# Database (direct connection for long-running jobs — no pooler timeout)
DATABASE_URL="postgresql://postgres:[password]@db.rvifkvvtqkpfmttcsany.supabase.co:5432/postgres"

# Current league name (must match poe.ninja league names exactly)
CURRENT_LEAGUE="Standard"

# Optional: Meilisearch for indexing after sync
MEILISEARCH_URL="http://localhost:7700"
MEILISEARCH_ADMIN_KEY="your-admin-key"
